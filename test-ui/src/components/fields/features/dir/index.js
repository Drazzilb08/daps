/**
 * Directory Features Export Module
 *
 * Composed components specific to directory field types.
 * These components orchestrate atomic primitives for directory-specific functionality.
 *
 * Usage:
 *   import { DirectoryArray } from '../features/dir';
 *
 * Design Philosophy:
 *   - Directory-specific composed functionality
 *   - Built using atomic primitives and shared components
 *   - Handles complex directory management business logic
 *   - Used by directory field types (dirlist, dirlist_dragdrop, etc.)
 */

export { DirectoryArray } from './DirectoryArray.jsx';
